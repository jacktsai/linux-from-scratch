#!/bin/bash
[ ! -e /etc/lfs-release ] && {
    echo 請在 LFS 環境執行!
    exit 0
}

function build() {
cd /tmp/build
rm -rf man-pages-4.09
tar -xf /tools/src/man-pages-4.09.tar.xz
cd man-pages-4.09

make install
}

time { build > /tmp/build/lastbuild.log; }
